import { GoldTracker } from '@/components/GoldTracker';

export const metadata = {
  title: 'Aurum Signal — Global Gold Flow Tracker',
  description: 'Track central-bank gold flows and market sentiment in one focused dashboard.',
};

export default function GoldTrackerPage() {
  return <GoldTracker />;
}
